package config

import "os"

// Config contiene ajustes básicos del servicio HTTP.
type Config struct {
	Port string
}

// Load lee PORT del entorno; por defecto 8080.
func Load() Config {
	port := os.Getenv("PORT")
	if port == "" {
		port = "8080"
	}
	return Config{Port: port}
}
