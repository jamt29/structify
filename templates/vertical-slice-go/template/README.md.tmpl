# {{ project_name | pascal_case }}

Vertical Slice en Go (MVP) con:
- `GET /health` en la feature `features/health`

## Ejecutar

```bash
make build
make run
```

- Ruta: `http://localhost:8080/health`
- Puerto configurable vía `PORT`

