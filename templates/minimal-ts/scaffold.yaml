name: "minimal-ts"
version: "0.0.1"
author: "structify"
language: "typescript"
architecture: "clean"
description: "Builtin minimal TypeScript template"
tags: ["typescript", "minimal"]
inputs:
  - id: project_name
    prompt: "Project name?"
    type: string
    required: true
    validate: "^[a-zA-Z][a-zA-Z0-9_-]*$"
files: []
steps: []

