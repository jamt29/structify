package repository

// {{ project_name | pascal_case }}Repository is an example repository interface.
// Implementations are provided as stubs for different ORMs via scaffold.yaml `when:` rules.
type {{ project_name | pascal_case }}Repository interface {
	GetByID(id string) (string, error)
	Save(id string, value string) error
}

