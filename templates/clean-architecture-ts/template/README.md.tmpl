# {{ project_name | pascal_case }}

Clean Architecture en TypeScript con un endpoint `GET /health`.

## Runtime
- `runtime={{ runtime }}`

## Ejecutar

```bash
npm install
npm run build
npm start
```

- `GET /health` responde `{ "status": "ok" }`

